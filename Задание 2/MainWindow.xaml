<Window x:Class="Задание_2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Задание_2"
        mc:Ignorable="d"
Title="Текстовый редактор" Height="600" Width="800" Background="LightGray">
    <DockPanel>
        <!-- Панель с кнопками "Открыть" и "Сохранить" -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Background="DarkSlateGray" Height="40">
            <Button Name="OpenButton" Content="Открыть" Width="80" Margin="5" 
                    Click="OpenButton_Click" Background="LightSteelBlue" Foreground="Black"/>
            <Button Name="SaveButton" Content="Сохранить" Width="80" Margin="5" 
                    Click="SaveButton_Click" Background="LightSteelBlue" Foreground="Black"/>
        </StackPanel>

        <!-- Текстовое поле для редактора -->
        <TextBox Name="EditorTextBox" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" 
                 HorizontalScrollBarVisibility="Auto" FontSize="14" Margin="10" Background="WhiteSmoke" />
    </DockPanel>
</Window>