<Window x:Class="Задание_1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Задание_1"
        mc:Ignorable="d"
        Title="Рисовалка" Height="600" Width="800" Background="LightGray">
    <Grid>
        <!-- Панель инструментов -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Background="DarkSlateGray" Height="50">
            <Button Name="LineButton" Content="Линия" Width="100" Margin="5" Background="LightSteelBlue" Foreground="White"/>
            <Button Name="CircleButton" Content="Круг" Width="100" Margin="5" Background="LightSteelBlue" Foreground="White"/>
            <Button Name="SquareButton" Content="Квадрат" Width="100" Margin="5" Background="LightSteelBlue" Foreground="White"/>
            <TextBlock Text="Выберите форму и нарисуйте на холсте" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="White"/>
        </StackPanel>

        <!-- Холст для рисования -->
        <Canvas Name="DrawingCanvas" Background="WhiteSmoke" Margin="10,60,10,10" />
    </Grid>
</Window>